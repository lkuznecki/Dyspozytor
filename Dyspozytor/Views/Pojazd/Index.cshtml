@model IEnumerable<Dyspozytor.Models.Pojazd>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa_Pojazdu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pojemnosc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa_Pojazdu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pojemnosc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PojazdID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PojazdID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PojazdID })
        </td>
    </tr>
}

</table>
